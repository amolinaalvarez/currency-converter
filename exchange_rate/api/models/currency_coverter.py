
class CurrencyConverter(object):

    def __init__(self, source_code, exchanged_code, amount, result):
        self.source_code = source_code
        self.exchanged_code = exchanged_code
        self.amount = amount
        self.result = result
